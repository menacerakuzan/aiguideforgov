/**
 * Урок 2.1 — тексти, які набираються в кадрі.
 *
 * Один файл на два застосування: суха репетиція (`2-1-dry.mjs`) прогоняє ті самі
 * тексти без запису, щоб звірити відповідь сервісу з тим, що написано в уроці,
 * а дубль (`2-1.mjs`) знімає їх уже начисто. Так у кадр не потрапить нічого,
 * чого ми не бачили заздалегідь.
 *
 * Джерело істини — `prisma/src/content/lessons-module-2.ts`,
 * урок `lyst-vidpovid-na-zvernennya`. Тексти звірені дослівно.
 */

/** Три абзаци звернення — рівно те, що в кадрі виділяють у Word і копіюють. */
export const APPEAL = `Прошу вжити заходів щодо дитячого майданчика у дворі нашого будинку. Гойдалки поламані, огорожа частково відсутня, гумове покриття провалилося.

Майданчиком щодня користуються діти з навколишніх будинків, і через відсутність огорожі вони вибігають просто на проїжджу частину. Вважаю, що це створює загрозу для їхнього життя та здоров'я.

Прошу повідомити, які заходи буде вжито та в які строки планується ремонт майданчика.`;

/**
 * Запит розділено на дві частини — не з примхи, а тому що так це відбувається
 * на екрані: людина пише початок, вставляє скопійоване звернення й дописує
 * решту. Склеєний PROMPT потрібен лише сухій репетиції.
 */
export const PROMPT_HEAD = `Підготуй проєкт офіційної відповіді на звернення мешканки.

Текст звернення:
"""`;

export const PROMPT_TAIL = `"""

Що вже зроблено: майданчик обстежено, складено дефектний акт
Наше рішення: ремонт заплановано на поточний квартал коштом програми благоустрою

Офіційно-діловий стиль, звертання на «Ви», до однієї сторінки.
Структура: підтвердження отримання → що зроблено → рішення і строк → контакт.
Дай відповідь на всі питання, порушені у зверненні.
Не вигадуй номерів документів, дат, прізвищ і посилань на закони — залиш замість них позначку [ЗАПОВНИТИ] з підказкою, що саме вписати.`;

export const PROMPT = `${PROMPT_HEAD}\n${APPEAL}\n${PROMPT_TAIL}`;

/**
 * Три уточнення поспіль — крок 4 уроку. Порядок має значення: спершу форма
 * й обсяг, потім те, чого помічник знати не міг, і лише тоді прибирання зайвого.
 */
export const REFINEMENTS = [
  'Оформи суцільним текстом, без підзаголовків. Скороти на третину, прибери загальні фрази, офіційний стиль збережи.',
  'Додай абзац про те, що до початку ремонту пошкоджене обладнання демонтують.',
  // Формулювання уточнене після сухої репетиції 2026-08-20: попереднє
  // («не пиши "коштом програми благоустрою"») прибирало три слова, а сама
  // згадка про програму лишалась у тому ж реченні.
  'Прибери згадку про програму благоустрою повністю — джерело фінансування в листі не вказуємо.',
];

/** Позначки, які в останньому кадрі проходимо маркером у бланку. */
export const CHECK_MARKS = ['[ЗАПОВНИТИ: вхідний номер]', '[ЗАПОВНИТИ: дата реєстрації]'];
