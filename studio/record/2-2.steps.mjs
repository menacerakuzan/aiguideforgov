/**
 * Урок 2.2 — тексти, які набираються в кадрі.
 *
 * Джерело істини — `prisma/src/content/lessons-module-2.ts`, урок
 * `skorochennya-velykoho-tekstu`. Промпт звірений дослівно з `promptBlock`
 * уроку; єдина зміна — [КОМУ] підставлено як «керівника», щоб у кадрі був
 * один конкретний, завершений приклад, а не шаблон із дужками.
 */

export const PROMPT = `Ось документ (файл додано). Зроби зведення на одну сторінку
для керівника.

Включи: головні цифри, ключові висновки, проблемні місця.
Для кожної цифри вкажи сторінку, з якої вона взята.
Бери лише те, що є в документі, нічого не додавай.`;
